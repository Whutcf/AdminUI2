<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.smic.cf.mapper.Covid19TrendHistMapper">

    <select id="getYesterdayForeignInIncr" resultType="com.smic.cf.crawlerbaidu.pojo.Covid19TrendHist">
        select id, name, date, series_name, value
        from adminui.sys_covid19_trend_hist
        where name = '中国疫情汇总'
          and series_name = '新增境外输入'
          and id = (select max(id)
                    from sys_covid19_trend_hist
                    where name = '中国疫情汇总'
                      and series_name = '新增境外输入')
    </select>
</mapper>